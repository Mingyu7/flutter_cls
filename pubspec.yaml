name: untitled1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 추가된 패키지
  get_it: 8.0.2
  dio: 5.7.0
  provider: 6.1.2
  uuid: 4.5.1
  table_calendar: 3.1.2
  intl: 0.19.0
  drift: 2.21.0
  drift_flutter: 0.1.0
  path_provider: 2.1.5
  path: 1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # 기존 파일 버전 ^5.0.0 사용

  # 추가된 개발 의존성
  drift_dev: 2.21.0
  build_runner: 2.4.13

flutter:
  uses-material-design: true